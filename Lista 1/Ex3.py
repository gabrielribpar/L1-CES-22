def sum_to(n):
    """Realiza a soma dos inteiro de 1 a n"""
    soma=0 #Variavel auxiliar para ter a soma dos valores
    for i in range(n+1): #Loop para realizar a soma dos valores
        soma=soma+i
    return soma
